import React from 'react';
import Header from '../components/Header';

const Add = () => {
    return(
        <div className="container">
            <Header />
            <h1>Add 画面</h1>
        </div>
    );
}

export default Add;