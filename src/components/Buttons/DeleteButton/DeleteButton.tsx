import React from 'react';
import './DeleteButton.scss';

const DeleteButton = () => {
    return(
        <button 
        className="delete-button"
        >
            削除
        </button>
    );
}

export default DeleteButton;