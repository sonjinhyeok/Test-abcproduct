import React from 'react';
import './AddButton.scss';

const AddButton = () => {
    return(
        <button 
        className="add__button"
        >
            登録
        </button>
    );
}

export default AddButton;