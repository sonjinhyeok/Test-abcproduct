import React from 'react';

const DeleteButton = () => {
    return(
        <button 
        className="delete-button"
        >
            削除
        </button>
    );
}

export default DeleteButton;